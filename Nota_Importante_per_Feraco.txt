NOTE FOR TOMMASO FERACO (IMPORTANT, READ FIRST)

You do NOT have access to the GPT project context, only to the GitHub repo.
So below I refer ONLY to files that exist in the repo, with their real paths.

Current status (what is stable now)
- The pipeline exports two tables used by the Shiny app:
  1) scoring/norms_lookup.csv
     measure | age | raw | p | z_raw | z_aligned | ss_1_19
  2) scoring/ss_to_raw_intervals.csv
     measure | age | ss_1_19 | raw_min | raw_max

- Integer (discrete) measures: some scaled scores (SS) can be unattainable at a given age.
  In scoring/ss_to_raw_intervals.csv this is represented by raw_min/raw_max = NA for those SS.
  This is intentional (human-readable, avoids mapping the same raw to multiple SS).

Where the scoring logic currently lives
- TR and TD scoring from PsychoPy/OpenSesame uploads is implemented in:
  shiny/app.R
  Look for the functions:
    score_tr_from_csv()
    score_td_from_csv()
  These functions currently compute ONE total raw score for TR and ONE raw threshold for TD.

Main pending work (your tasks)
============================================================

1) Time Reproduction (TR): score each target duration separately, then aggregate at the z/SS level
---------------------------------------------------------------------------------------------

Goal
- Instead of computing only one TR raw (mean absolute percent deviation across all trials),
  compute 11 duration-specific TR raws (2..12 seconds) and standardize them separately.
  Then aggregate standardized values to obtain the TR total used in the profile and in SoTQ.

Files you must edit
A) Shiny preprocessing (trial-level + per-duration)
- File: shiny/app.R
- Modify score_tr_from_csv() so that it returns (or internally computes) trial-level data:
    target (seconds), response (seconds), devabs (%)
  using the SAME parsing already present in that function.

The current app already handles two TR formats inside score_tr_from_csv():
1)  Builder-style  (Task1..Task11 columns)
   - It already contains the target mapping:
     Task1=8, Task2=5, Task3=2, Task4=6, Task5=12, Task6=4,
     Task7=9, Task8=10, Task9=3, Task10=11, Task11=7
   - It already extracts a duration column (key_resp_10.duration or first duration-like col)

2)  Coder-style 
   - It already expects columns (type, duration, reproducedTime) and removes practice

Validity filter (already in shiny/app.R, keep it identical)
- devabs = 100 * abs(response - target) / target
- keep trials with response > 0.100 AND response < 36

New per-duration outputs you need
- For each target t in {2,3,...,12}:
    PercDevAbs_TR_t = mean(devabs for target == t)
- Recommended: if ANY duration has 0 valid trials, flag TR as incomplete and do not compute TR total.

B) Norms export for TR durations (so Shiny can lookup z/SS for each duration)
- Files:
  - scoring/measure_specs.csv   (add the TR duration measures)
  - R/02_export_norms_to_tables.R (re-run after updating measure_specs)

What to add in scoring/measure_specs.csv
- Add measures:
    PercDevAbs_TR_2, PercDevAbs_TR_3, ..., PercDevAbs_TR_12
  with:
    raw_type = continuous
    direction_flag = lower_better
    abs_min = 0
    abs_max = Inf
    raw_grid_n ~ 201

Then re-run:
  R/02_export_norms_to_tables.R
and verify in:
  scoring/norms_lookup.csv
that PercDevAbs_TR_2..12 exist across ages.

C) TR total standardized score (what Shiny should use)
- For a given child:
  1) lookup z_aligned_t for each PercDevAbs_TR_t using scoring/norms_lookup.csv
     (direction is already handled in z_aligned by construction)
  2) TR_z_total = mean(z_aligned_2..12)  [only if all 11 are available]
  3) TR_SS_total = round(10 + 3 * TR_z_total), clamped to [1,19]
- Profile plot: show only TR total (aggregated), optional debug table for the 11 items.

2) Reliability of the overall composite is catastrophically low (needs investigation)
-------------------------------------------------------------------------------

Observed problem
- The current reliability estimation produced extremely low/negative reliability
  (alpha negative and omega very low), leading to a huge SEM and very wide CI.

This is a blocking psychometric issue.
Do NOT rely on scoring/SoTQ_CI_params.csv for CI/SEM until this is explained.

Where to work
- File: R/05_estimate_SoTQ_CI_params.R
- Data used by that script are in the repo folder:
  data/Database_Time_*.xlsx
  and the dictionary: data/Data_Dictionary.xlsx

Minimum diagnostic checklist
A) Inspect correlation structure after norming
- In R/05_estimate_SoTQ_CI_params.R print:
  - cor(Z)
  - item-total correlations
  Confirm most covariances are not negative after age-standardization.

B) Verify directionality and variable mapping end-to-end
- Confirm raw ranges are plausible:
  * TE deviation >= 0
  * TR devabs >= 0
  * TD ratio typically >= 1
  * questionnaires totals within bounds
- Spot-check that increasing  error  raw implies decreasing z_aligned (lower_better).

C) Recompute reliability after implementing the paper-consistent TR total
- Currently reliability uses the aggregated TR raw.
- After moving to item-level TR standardization and TR_z_total, re-run reliability.
  It may improve substantially, or it may reveal deeper issues in the battery structure.

If reliability remains negative/near-zero
- Alpha may be meaningless if average covariance is negative.
- Decide explicitly what to do for CI:
  * switch to omega_total-based SEM, or
  * clamp reliability at 0 for CI computation (conservative), and report the problem.

Summary of files you will touch
- shiny/app.R  (TR per-duration scoring + aggregation)
- scoring/measure_specs.csv  (add PercDevAbs_TR_2..12)
- R/02_export_norms_to_tables.R  (re-run export)
- R/05_estimate_SoTQ_CI_params.R (re-check reliability; likely revise)

End.
